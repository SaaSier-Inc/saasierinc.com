document.addEventListener('DOMContentLoaded',function(){const mobileMenuBtn=document.querySelector('.mobile-menu-btn'),navMenu=document.querySelector('.nav-links');mobileMenuBtn&&navMenu&&(mobileMenuBtn.addEventListener('click',()=>{mobileMenuBtn.classList.toggle('active'),navMenu.classList.toggle('active'),document.body.style.overflow=navMenu.classList.contains('active')?'hidden':''}),document.querySelectorAll('.nav-links a').forEach(link=>{link.addEventListener('click',()=>{mobileMenuBtn.classList.remove('active'),navMenu.classList.remove('active'),document.body.style.overflow=''})})),document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener('click',function(e){const targetId=this.getAttribute('href');if(targetId.length>1){e.preventDefault();const targetElement=document.querySelector(targetId);targetElement&&window.scrollTo({top:targetElement.offsetTop-80,behavior:'smooth'})}})}),window.addEventListener('scroll',function(){const header=document.querySelector('header'),backToTopBtn=document.getElementById('back-to-top');window.scrollY>100?(header.classList.add('scrolled'),header.style.background='rgba(255, 255, 255, 0.98)'):(header.classList.remove('scrolled'),header.style.background='rgba(255, 255, 255, 0.95)'),backToTopBtn&&(window.scrollY>500?backToTopBtn.classList.add('visible'):backToTopBtn.classList.remove('visible'))});const backToTopBtn=document.getElementById('back-to-top');backToTopBtn&&backToTopBtn.addEventListener('click',()=>{window.scrollTo({top:0,behavior:'smooth'})});const observerOptions={root:null,rootMargin:'0px',threshold:.1},observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{entry.isIntersecting&&entry.target.classList.add('animate-in')})},observerOptions);document.querySelectorAll('.company-card, .news-item, .goal, .value-card, .trust-item, .phase, .stat').forEach(el=>{observer.observe(el)});const heroVisual=document.querySelector('.hero-visual');heroVisual&&(window.addEventListener('scroll',()=>{const scrolled=window.pageYOffset,parallax=scrolled*.4;heroVisual.style.transform=`translateY(calc(-50% + ${parallax}px))`}),window.addEventListener('mousemove',e=>{const x=e.clientX/window.innerWidth,y=e.clientY/window.innerHeight;heroVisual.querySelectorAll('.floating-card').forEach((card,index)=>{const speed=(index+1)*5,offsetX=(x-.5)*speed,offsetY=(y-.5)*speed;card.style.transform=`translate(${offsetX}px, ${offsetY}px)`})}))});document.querySelectorAll('.company-card, .news-item, .goal, .value-card, .trust-item, .phase, .stat').forEach((el,index)=>{el.style.animationDelay=`${(index%5)*.1}s`});
